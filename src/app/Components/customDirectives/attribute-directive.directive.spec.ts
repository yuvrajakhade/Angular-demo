import { AttributeDirective } from './attribute-directive.directive';

describe('AttributeDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeDirective();
    expect(directive).toBeTruthy();
  });
});
